var should = require('should');
describe('Socket', function() {
  it('Should broadcast new user to all users: mock ', function (done) {
    [1,2,3].indexOf(5).should.equal(-1);
    [1,2,3].indexOf(0).should.equal(-1);
    done();
  });
});
